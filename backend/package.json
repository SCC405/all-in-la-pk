{
  "name": "all-in-la-pk-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API REST de All In La PK",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/app.js && node --check src/config/database.js && node --check src/config/env.js && node --check src/routes/health.routes.js && node --check src/middlewares/not-found.js && node --check src/middlewares/error-handler.js && node --check test/app.test.js && node --check test/database.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^18.0.4",
    "express": "^5.2.1",
    "mongoose": "^8.24.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
